
function initMoviePlayer(videoId,overlayId,src){var video=document.getElementById(videoId),overlay=document.getElementById(overlayId),loaded=false,hls=null;if(!video||!overlay)return;function attach(){if(loaded)return;loaded=true;if(video.canPlayType('application/vnd.apple.mpegurl')){video.src=src}else if(typeof Hls!=='undefined'&&Hls.isSupported()){hls=new Hls({enableWorker:true,lowLatencyMode:false});hls.loadSource(src);hls.attachMedia(video)}else{video.src=src}}function play(){attach();overlay.classList.add('is-hidden');var p=video.play();if(p&&p.catch){p.catch(function(){overlay.classList.remove('is-hidden')})}}overlay.addEventListener('click',play);video.addEventListener('click',function(){if(video.paused)play()});video.addEventListener('play',function(){overlay.classList.add('is-hidden')});video.addEventListener('pause',function(){if(!video.ended)overlay.classList.remove('is-hidden')});window.addEventListener('pagehide',function(){if(hls&&hls.destroy)hls.destroy()})}
